# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Zupreme' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for Zupreme
  pod 'Zupreme' # The Zilliqa Swift SDK
  pod 'JSONRPCKit', '~> 4.0', :git => 'github.com/ollitapa/JSONRPCKit.git'
  pod 'SwiftLint' # Code convention linter
  pod 'SwiftyBeaver' # Logging in the app
  pod 'TinyConstraints'
  pod 'FormValidatorSwift'
  pod 'DeallocationChecker'
  pod 'RxCocoa'
  pod 'RxDataSources'

  target 'ZupremeTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
